{
  "name": "ji-daw-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --open /index.source.html",
    "build": "tsc --noEmit && vite build && node scripts/publish-root.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@gwegash/sf2-player": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.7"
  }
}
